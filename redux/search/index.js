/*
 * Built by Astrocoders
 * @flow
 */

import { compose, pure } from 'recompose'

import container from './container'

const Search = () => {}

Search.propTypes = {}

export default compose(container, pure)(Search)
